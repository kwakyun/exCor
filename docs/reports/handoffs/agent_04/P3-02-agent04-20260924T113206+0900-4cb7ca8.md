# P3-02: Hardhat 구현과 로컬 배포 인계

- agent_id: agent_04
- work_item_id: P3-02
- parent_tasks: [P3-02]
- run_id: agent04-20260924T113206+0900-4cb7ca8
- status: ready_for_review
- base_commit: 4cb7ca83069f58103707426f215de5c96bddfe01
- result_commit_or_patch: shared checkout에 아래 신규 파일. commit 없음.
- changed_files: `chain/package.json`, `chain/package-lock.json`, `chain/tsconfig.json`,
  `chain/hardhat.config.ts`, `chain/contracts/AlleyStampRegistry.sol`,
  `chain/ignition/modules/AlleyStampRegistry.ts`, `chain/scripts/generateDeploymentMetadata.ts`,
  `chain/deployments/local-31337.json`, `chain/.gitignore`, 이 handoff.
  (`hardhat.config.sandbox.ts`는 04의 클라우드 검증 샌드박스 전용이며 이 인계에 포함하지
  않았다 — 아래 "환경 차이" 참조.)
- contract_versions: hardhat 3.17.0, @nomicfoundation/hardhat-toolbox-viem 5.0.7,
  @nomicfoundation/hardhat-ignition-viem 3.1.6, @openzeppelin/contracts 5.6.1, viem 2.56.8,
  solidity 0.8.28
- acceptance_results:
  - "정상 claim/hasStamp 로컬 동작": 실제 로컬 Hardhat 노드(`hardhat node`)에 Ignition으로
    배포 후 실제 tx로 검증(아래 verification 참조). 단위 테스트로도 반복 검증
    (`docs/reports/onchain/security-gas.md`).
  - "단일 metadata를 UI/API에 제공": `chain/scripts/generateDeploymentMetadata.ts`가 컴파일
    artifact의 ABI를 그대로 옮겨 `chain/deployments/local-<chainId>.json` 하나만 생성한다.
    다른 모듈에 주소/ABI를 수동 복사하지 않는다(원칙은 `03-onchain-system.md` 7절).
- verification_commands_and_results:
  - `cd chain && npm install && npm run compile` — 04의 클라우드 샌드박스에서는 조직
    egress 정책으로 `binaries.soliditylang.org` 직접 다운로드가 차단되어, npm의 `solc`
    패키지를 직접 가리키는 `hardhat.config.sandbox.ts`(비배포용)로 컴파일했다. 01/실제
    개발 환경(사용자 PC)에서는 `hardhat.config.ts`의 표준 경로로 재현해야 한다 — 이 환경
    차이를 remaining_risks에 명시한다.
  - `cd chain && npx hardhat ignition deploy ignition/modules/AlleyStampRegistry.ts --network localhost`
    → `AlleyStampRegistryModule#AlleyStampRegistry - 0x5FbDB2315678afecb367f032d93F642f64180aa3`
    (deployment block 1).
  - `cd chain && npx tsx scripts/generateDeploymentMetadata.ts 31337 0x5FbDB2315678afecb367f032d93F642f64180aa3 1`
    → `chain/deployments/local-31337.json` 생성 확인.
  - 실제 tx 스모크: admin(#0)이 `setCampaign`/`setCampaignSpot`/`setIssuer` 호출 →
    issuer(#1, 니모닉 파생)가 EIP-712 서명 → recipient(#2)가 `claim()` 제출 →
    `claim tx: 0x164aa08520eb927a49a251513c84310b3a5c5a52a95e7e33633f417d77876843 status: success block: 5`,
    `hasStamp` true. 개인키 하드코딩 없음(Hardhat 표준 니모닉의 addressIndex 파생).
- artifacts: `chain/deployments/local-31337.json`(주소/chainId/시작블록/ABI 전체),
  `chain/ignition/modules/AlleyStampRegistry.ts`.
- dependency_requests: (P3-01 handoff의 02용 ABI/wallet fixture 전달과 동일 — 중복 기록하지
  않음, 그 문서 참조.)
- remaining_risks:
  - **환경 차이**: 04의 클라우드 검증 샌드박스는 egress 제약으로 solc를 npm 패키지 경로로
    받았다. 사용자 PC(실제 개발 환경)에서 `npm run compile`이 표준 다운로드 경로로 정상
    동작하는지는 01/사용자 환경에서 별도 확인이 필요하다 — 04는 이 경로 차이를 스스로
    확정할 수 없다.
  - Solidity `production` 프로파일(optimizer 활성화)로는 아직 컴파일/배포/가스 측정을 하지
    않았다.
  - 공개 테스트넷 배포는 이번 실행 범위 밖이다(04-onchain.yaml: "공개 배포는 해당 실행
    요청이 있어야 한다").
- next_actions: 01이 사용자 PC 환경에서 `chain/hardhat.config.ts`(표준 설정)로
  `npm install && npm run compile && npm test`를 독립 재현. 통과하면 P3-03/P3-04/P3-06과
  함께 accepted_handoffs 등재 검토.
