# ENV-01 인계 — 공유 실행 환경과 의존성 창구

- agent_id: agent_02
- work_item_id: ENV-01
- parent_tasks: []
- run_id: agent02-20260924T100752+0900-4cb7ca8
- status: ready_for_review
- base_commit: 4cb7ca83069f58103707426f215de5c96bddfe01 (working tree, uncommitted — 공유 checkout, 01 원장 기준)
- result_commit_or_patch: 커밋 없음(공유 checkout 정책). 변경은 working tree에 존재.
- changed_files:
  - package.json (viem 2.56.8 dependency, tsx/esbuild devDependency, scripts 추가)
  - package-lock.json
  - vite.config.ts (server.port/host 명시)
  - tsconfig.json (ES2022.Error lib 추가, jobs 포함)
  - scripts/smoke-api.mjs (신규)
  - docs/runbooks/development.md (신규)
  - package.json / package-lock.json — @electric-sql/pglite 0.5.8 devDependency 추가 (03 요청, 아래 참조)
- contract_versions: 해당 없음(환경/실행 작업)
- acceptance_results:
  - "루트/chain 실행 경계 기록": PASS — docs/runbooks/development.md에 chain/**는 04 단독,
    루트 Vitest에서 chain/** 제외를 기록.
  - "명령 실행 증거": PASS — 아래 verification_commands_and_results 참조.
  - "실제 secret 없이 placeholder 설정": PASS — 새 환경변수/secret 추가 없음, PORT만 사용.

## verification_commands_and_results (2026-09-24 16:4x KST, Node 24.15.0 / npm 11.12.1)

| 명령 | 결과 |
|---|---|
| `npm ci` | 이전 세션에서 완료 확인(node_modules 존재, 이번 세션에서 재실행하지 않음 — 다른 에이전트와 동시 루트 install 금지 규칙 준수) |
| `npm run typecheck` | **FAIL** — `src/server/controllers/stamps/stampsController.ts`(agent_04 소유) 미사용 선언 2건. 02 소유 파일은 오류 없음. |
| `npm test` (vitest) | PASS — 90/90 (14 파일). P1-02 신규 20개 포함. |
| `npm run test:contracts` | PASS — 6/6 |
| `npm run build` (`tsc && vite build`) | **FAIL** — 위와 같은 typecheck 오류로 중단. `npx vite build` 단독 실행은 PASS(1920 modules, dist 349.68kB/gzip 98.25kB) — 즉 프런트엔드 번들 자체는 문제 없고 tsc 게이트가 agent_04 파일 때문에 막혀 있다. |
| `node scripts/smoke-api.mjs` | **FAIL(신규 회귀, 02 소유 아님)** — API startup timeout(10s). `npx tsx src/server/server.ts` 직접 실행도 8초간 아무 출력 없이 행. `src/server/app.ts`가 이번 세션 중 +190줄 변경됨(agent_03 소유, 최근 mtime). ENV-01 최초 작성 시점(이전 세션)에는 이 smoke가 PASS했다 — 그 이후 app.ts 변경으로 회귀한 것으로 보이며 02 소유 파일 변경은 없었다. |
| dist/ 삭제 권한 | 최초 `vite build`가 `EPERM: operation not permitted, unlink dist/assets/...`로 실패 — 사용자 승인으로 해당 폴더 삭제 권한을 받은 뒤 정상화(이 세션에서 1회성으로 해결, 코드 변경 아님). |

## artifacts
- docs/runbooks/development.md (기존 세션에서 작성, 이번에 명령 재검증)
- docs/reports/performance/engine-contract.md (P1-02와 공유, 4/5절에 ENV 관련 매핑 설명 포함)

## 처리한 03/04 패키지 요청

- **03 → 02 (P3-DB 인계, `@electric-sql/pglite` devDependency 요청)**: 03이 세션 중
  `npm install @electric-sql/pglite --no-save`로 임시 설치해 쓰고 있었고(공유 checkout에서
  다른 담당이 `npm ci`/`npm install`을 돌리면 사라지는 상태), `pgliteSqlClient.ts`/두
  `*.pglite.test.ts`가 이를 `describe.skipIf`로 미설치 시 조용히 건너뛰고 있었다. 02가
  루트 package/lockfile 소유자로서 `npm install --save-dev --save-exact
  @electric-sql/pglite@0.5.8`로 정식 반영했다(현재 03이 검증한 버전과 동일 버전 고정).
  검증: `npx vitest run src/server/repositories/__tests__` PASS 8/8(pglite 실제 DB 테스트
  둘 다, skip 없이 실행됨). 순수 devDependency(WASM, 네이티브 바이너리 없음)이며 런타임
  프로덕션 코드는 여전히 `SqlClient` 구조적 인터페이스만 의존한다(03의 설명 그대로).

## dependency_requests
- request_id: ENV-01-BUILD-BLOCK-01, from: agent_02, to: agent_04 (경유 agent_01) —
  `src/server/controllers/stamps/stampsController.ts`의 미사용 import/선언 2건이
  `npm run typecheck`/`npm run build`를 막고 있습니다(TS6196 'Hex', TS6133
  'StampsApplicationError'). 02 소유 파일이 아니라 직접 수정하지 않았습니다. 제거 또는
  실제 사용으로 정리 요청.
- request_id: ENV-01-API-REGRESSION-01, from: agent_02, to: agent_03 (경유 agent_01) —
  `node scripts/smoke-api.mjs`/`npx tsx src/server/server.ts`가 이번 세션 중 무응답으로
  타임아웃됩니다. `src/server/app.ts`가 최근 +190줄 변경되었습니다(제가 관찰한 시점 기준
  mtime 최신). 어떤 모듈이 import 시점에 블로킹되는지 확인 요청(예: DB 커넥션을 동기적으로
  기다리는 초기화 코드가 있는지). runbook의 "독립 API 실제 검증" 문구는 이 회귀가 해소될
  때까지 최신 상태가 아님을 알려드립니다 — 제가 직접 app.ts/server.ts를 수정하지는
  않았습니다(소유권 밖).

## remaining_risks
- `npm run build` 전체는 여전히 실패 상태(agent_04 소유 파일 원인). 02 소유 범위(vite
  번들)는 단독 검증 PASS.
- smoke-api 회귀는 원인 미확인 상태로 남겨둔다 — 반복 실행/추측 수정을 하지 않았다.

## next_actions
- 01: 위 두 dependency_request를 각 소유자(04/03)에게 배정.
- 02: P1-02로 진행(본 인계와 동시 제출). ENV-01은 이 상태로 review 요청.
