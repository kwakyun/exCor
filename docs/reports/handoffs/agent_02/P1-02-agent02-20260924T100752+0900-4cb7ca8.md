# P1-02 인계 — 정확한 예산 도메인 (엔진/타입/fixture → 03에 우선 인계)

- agent_id: agent_02
- work_item_id: P1-02
- parent_tasks: [P1-02]
- run_id: agent02-20260924T100752+0900-4cb7ca8
- status: ready_for_review
- base_commit: 4cb7ca83069f58103707426f215de5c96bddfe01 (working tree, uncommitted — 공유 checkout)
- result_commit_or_patch: 커밋 없음(공유 checkout 정책, 01 원장 기준 hash로 고정 예정)
- changed_files:
  - src/domain/types.ts (신규)
  - src/domain/validation.ts (신규)
  - src/domain/budgetEngine.ts (신규 — buildPlan, buildCheapestPlan)
  - src/domain/swapPlanSpot.ts (신규)
  - src/domain/catalogAdapter.ts (신규)
  - src/domain/index.ts (신규, 배럴)
  - src/domain/__tests__/budgetEngine.fixture.test.ts (신규 — core-v1.json 전체 case/swapCase)
  - src/domain/__tests__/budgetEngine.extra.test.ts (신규 — all 권역/빈 candidateSpotIds/swap 부가 오류)
  - src/services/budgetCalculator.ts (수정 — 내부를 src/domain에 위임하는 facade로 교체, App.tsx
    시그니처/TravelPlan 모양은 그대로 유지)
  - src/services/__tests__/budgetCalculator.test.ts (기존 5개 유지 + 신규 5개 회귀 테스트 추가)
  - src/types/index.ts (TravelPlan에 옵션 필드 `feasibility?: PlanFeasibility` 추가 — 추가 전용)
  - docs/reports/performance/engine-contract.md (신규 — 결정 기록)
- contract_versions: core-v1.0.0-rc2 (docs/contracts/core-v1.md, accepted_handoffs 기준)
- acceptance_results:
  - "총액/잔액·정수·인원 경계·입력 불변성 테스트": PASS — fixture 6 cases + 4 swapCases
    (아래), `sum(items.costKRW)+transitCostKRW=totalSpent`/`totalSpent+remainingBudget=budgetKRW`
    불변식 테스트, 입력/catalog 불변성 테스트, budgetKRW/partySize 범위 밖 invalid 테스트 포함.
  - "동일 입력과 버전은 동일 장소·금액": PASS — 결정론 테스트(`buildPlan(input,catalog)`를
    두 번 호출해 `toEqual` 확인).

## verification_commands_and_results (2026-09-24 16:4x KST)

| 명령 | 결과 |
|---|---|
| `npx vitest run src/domain/__tests__` | PASS 20/20 (fixture 12 + extra 8) |
| `npx vitest run src/services/__tests__/budgetCalculator.test.ts` | PASS 10/10 (기존 5 + 신규 회귀 5) |
| `npm test` (전체) | PASS 90/90, 14 파일 — 03/04 소유 테스트 포함, 내 변경으로 인한 회귀 없음 |
| `npm run test:contracts` | PASS 6/6 (fixture 자체 산술 검사, 변화 없음) |
| `npm run typecheck` | 내 파일(src/domain/**, src/types, src/services) 오류 0건. 잔여 오류는
    agent_04 소유 `src/server/controllers/stamps/stampsController.ts` 2건뿐(ENV-01 인계에 기록). |
| `npx vite build` | PASS — 1920 modules, dist 349.68kB / gzip 98.25kB |

### fixture 케이스별 결과 (docs/contracts/fixtures/core-v1.json, core-v1.0.0-rc2)

| case id | expected.status | 결과 |
|---|---|---|
| two-person | ok (totalSpent 28200) | PASS |
| vehicle-ceiling | ok (totalSpent 76650, 혼합교통 ceil(5/4)=2대) | PASS |
| below-minimum | infeasible, minimumRequiredKRW 28200, scope catalog | PASS |
| missing-category | infeasible, scope candidates, minimumRequiredKRW 없음 | PASS |
| invalid-party | invalid, fields.partySize OUT_OF_RANGE | PASS |
| zero-budget | invalid, fields.budgetKRW OUT_OF_RANGE | PASS |
| swap: cross-district | invalid, originalUnchanged | PASS |
| swap: cross-category | invalid, originalUnchanged | PASS |
| swap: over-budget-swap | infeasible, additionalBudgetKRW 2200, originalUnchanged | PASS |
| swap: unknown-id | invalid, originalUnchanged | PASS |

fixture 정답과 엔진 구현을 같은 입력으로 직접 실행해 비교했다(fixture 자체 산술 검사인
`test:contracts`와는 별개). "증거 없는 미실행을 PASS로 쓰지 않는다" 원칙에 따라 위 표는
실제 `npx vitest run` 실행 결과다.

## artifacts
- src/domain/** (03/04가 바로 import 가능한 공유 타입/엔진/facade)
- docs/reports/performance/engine-contract.md — all 권역 처리, 목적함수/동점 규칙, production
  catalog 매핑, facade의 defect-fix 범위를 03/01이 검토할 수 있게 기록.

## 03에 인계하는 인터페이스 (즉시 사용 가능)

```ts
import {
  buildPlan, swapPlanSpot, buildProductionCatalogSnapshot,
  type PlannerInput, type CatalogSnapshot, type PlanV2, type PlanningResult, type SwapResult,
} from '../domain'; // 또는 03의 위치 기준 상대경로
```

`buildPlan`/`swapPlanSpot`는 core-v1.md와 100% 동일 시그니처다. `src/shared/api/dto.ts`의
PlannerInputDTO/CatalogSnapshotDTO/PlanV2ItemDTO 등은 이 파일들의 타입과 필드명·형태가
동일하므로 구조적으로 호환된다(같은 리터럴 유니온을 쓰도록 맞췄다) — 다만 dto.ts는 여전히
독립적으로 리터럴을 재선언하고 있으므로, 중복을 없애려면 03이 원하는 시점에 dto.ts에서
`src/domain/types`를 import하도록 정리하는 것을 제안한다(선택 사항, 강제 아님. dto.ts는
03 소유라 02가 직접 수정하지 않았다).

## dependency_requests

(ENV-01 인계와 중복 게시하지 않기 위해 ENV-01-BUILD-BLOCK-01 / ENV-01-API-REGRESSION-01은
그쪽 문서를 참조 바란다. P1-02 고유 요청은 없음 — 03에 필요한 것은 모두 위 "인계하는
인터페이스"로 이미 제공했다.)

## remaining_risks
- all 권역의 테마 가중치는 순수 엔진에 없다(engine-contract.md 2절). 제품이 이를 원하면
  CatalogSnapshot에 권역 메타데이터 확장이 필요하며 core-v1 다음 revision 대상이다.
- `buildCheapestPlan`은 core-v1 계약 밖의 02 전용 확장이며 `remainingBudget`이 음수일 수
  있다 — 03/04가 이 함수를 core-v1 API로 오인해 가져다 쓰지 않도록 문서화해 두었다
  (engine-contract.md 4절, budgetEngine.ts 상단 주석).
- P1-03(탐색 성능 최적화)은 아직 시작하지 않았다 — 현재는 brute force 참조 구현이며 실제
  성능 측정/개선은 다음 작업이다.
- SHARE-FIX-02의 나머지 절반(저장 실패 시 가짜 성공 링크, localStorage v2 검증)은 P1-04
  범위이며 이번에 다루지 않았다.

## next_actions
- 01: P1-02 accepted_handoffs 기록 및 03에 실제 연결 시작 승인.
- 03: `src/domain`을 P1-06(엔진 실연결)에서 import해 mock engine을 대체. TTL/DTO 세부는
  기존 dependency-requests.md의 BACKEND-HANDOFF-03 흐름을 따른다.
- 02: 다음은 priority_queue상 P1-03(탐색 성능) 또는 03/04의 패키지 요청 — 짧은 요청을
  먼저 처리하고 P1-03으로 이동 예정.
