# P1-07 독립 QA

run_id agent01-20260924T100749+0900-4cb7ca8 · base_commit 4cb7ca83069f58103707426f215de5c96bddfe01
통합 게이트 **BLOCKED**, 현재 제품 정확성 사전 QA **FAIL** — P1-03/P1-04/P1-06 accepted handoff 없음. 통합 commit 미확정. 아래는 현재 코드의 검토와 실제 실행이다.

환경: Windows / PowerShell / Node v24.15.0 / npm 11.12.1. 작업 경로는 assignments.md 참조.
`npm.cmd test`: exit 1, vitest 명령 없음. `npm.cmd run build`: exit 1, tsc 명령 없음. raw: evidence/baseline-test.log, baseline-build.log. 처음 로그 디렉터리 생성은 sandbox Access denied로 테스트 실행 전 실패했고, 허용된 실행에서 디렉터리 생성 후 위 두 명령을 실제 실행했다. 의존성 설치는 소유자02에게 ENV-BASELINE-02로 인계했다.

02 설치 후 환경 차단은 해소되었다. 01 독립 재실행: 기존 Vitest 10/10 PASS, build PASS (JS343.47kB/gzip96.03kB). evidence/post-install-test.log, independent-build.txt 참조. 최초 독립 실행에서 01의 Node .test.mjs가 Vitest에 수집되어 suite 없음 실패가 있었고 .check.mjs로 변경 후 재실행 exit0. 제품 테스트 실패와 구분한다.

## Required 발견

| 담당/파일 | 재현 입력/조건 | 현재 코드 | 기대 |
|---|---|---|---|
|02 budgetCalculator.ts generateTravelPlan|동일 30000원/전포/인원1→2|partySize가 비용 계산에 사용되지 않음|검수된 인당/차량 규칙 적용|
|02 같은 파일 fallback|예산1원|최저 조합 plan을 반환하고 remainingBudget를 0 clamp|infeasible, 합계 초과 plan 금지|
|02 같은 파일 swapSpotInPlan|전포 cafe 자리 → 다른 권역 food ID|ID 존재만 확인 후 비용/spot 치환|권역/카테고리 거절, 원본 유지|
|02 planApi.ts catch|저장 fetch reject|success:true, local-* 반환|실패와 로컬 저장 표시, 공유 성공 금지|
|03 planController.ts createPlan|body.plan 존재하나 합계/ID 조작|savePlan으로 그대로 전달|catalog/엔진 재검산 후 거절|
|03 planStorage.ts|저장 후 프로세스 재시작|Map 저장|DB 복원|
|02 App.tsx|손상 localStorage/빠른 입력|생성·로컬 저장·confetti 결합|검증된 migration 및 최신 결과만 적용|

요청: ENGINE-FIX-02, SHARE-FIX-02, BACKEND-HANDOFF-03.

추가 실행 `node tests/e2e/p1-current.probe.mjs`: exit1, 아래 6건 재현. evidence/legacy-defects.log에 실제 값 기록. 제품 코드를 수정하지 않고 esbuild 메모리 bundle에서 실행했으며 fake fetch/합성 자료/새 module Map만 사용했다.

- 인원1/2 모두 totalSpent=29600.
- 예산1원에 totalSpent=17600, remainingBudget=0인 정상 plan 반환.
- 전포 cafe jp-c3 자리에 영도 food yd-f1 교체 허용.
- 위조 totalSpent=-123을 controller가 HTTP status201 결과로 수용(실네트워크 HTTP 테스트는 아님).
- 저장 후 새 module state에서 조회404. 실제 프로세스/DB 재시작 테스트의 대체 통과 근거가 아닌 Map 유실 재현이다.
- fetch reject 후 success=true/local slug=true.

App.tsx 브라우저 흐름은 정적 검토만 수행했으며 실제 UI 검증은 미실행이다. 01 작성 probe도 02 교차 검토를 요청한다.

## 인계 후 실행할 AC

1. core fixture + 정수/1..8 인원/무료/빈 카테고리/예산초과/결정성/입력 불변성, 작은 exhaustive reference와 최적화 동일성.
2. 실제 HTTP v1/legacy 저장·조회, 잘못된 ID/합계, 같은 키 동시 요청/다른 본문409, 과거 catalog, DB 중단503. 별도 DB에서 서버 재시작 후 slug snapshot 동일.
3. 브라우저 입력20회/탭 왕복/손상 저장/공유 실패/재접속. mock과 실API 분리.
4. 원본/1000/10000 장소 분포, warmup10/100회/2초 timeout 포함, 동일 환경/fixture/엔진 연결 확인. 1000 p95<=100ms, API 동시10/1만 계획 p95<=300ms, gzip 전후 기록. 측정값 없음(N/A), 목표값을 측정으로 쓰지 않는다.

fixture 자체 검사 `node --test tests/e2e/c0-fixtures.check.mjs` 4 PASS는 C0 산술/형식 검증이며 P1 통과가 아니다. 02 교차 검토 대기.
