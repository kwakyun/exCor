# P3-07 독립 QA

run_id agent01-20260924T100749+0900-4cb7ca8 · base_commit 4cb7ca83069f58103707426f215de5c96bddfe01
상태 **BLOCKED** — P3-03/P3-05/P3-06 인계 없음. 최초 검사 chain package/ABI/deployment/repository/indexer 미존재. 로컬 발급, reorg, 가스, 공개 테스트넷 모두 미실행.

CLAIM-HANDOFF-04 및 BACKEND-HANDOFF-03 요청. 입력 명세 claim-v1.json은 합성 주소이며 배포 주소나 해시 결과가 아니다.

| 시나리오 | 기대/증거 | 현재 |
|---|---|---|
|승인→지갑 발급→중복거절→재접속→공개검증|API payload/receipt/event/hasStamp/projection 일치|미실행|
|typed data|02 wallet/03 DTO/04 signer/contract의 동일 입력 digest|해시 vector 미인계|
|변조/재사용/다른 domain/제3자/폐기 issuer|revert와 상태 불변|미실행|
|deadline 직전/동일/직후|허용/허용/거절|fixture 자체 경계 검사만 통과|
|미승인/다른 지갑/동시 요청|승인 차단, 멱등/중복 최대1회|미실행|
|pause/권한 관리|비인가 변경 실패, pause 중 claim 실패|미실행|
|재수집/재시작/reorg|별도 DB/node에서 canonical 복원 및 이전 확정 취소|미실행|
|RPC 장애/지갑전환|승인 보류, stale 시각, 이전 지갑 상태 분리|미실행|
|gas|claim/관리/실패 경로별 실제 snapshot|미측정|

DB projection 재구축은 03이 발급한 별도 테스트 namespace와 04의 예약 노드에서 수행한다. 공유 노드 reset/다른 DB clear 금지. 실제 방문 보증·공개 인증·공개 배포 승인을 로컬 mock으로 대신하지 않는다. 신뢰 경계와 운영 미결정은 claim-v1.md 참조. 02 교차 검토 대기.

## 01 독립 provisional 코드 probe (2026-09-24 15:28 KST)

`tests/e2e/p3-current.probe.mjs` 신규 작성·실행 (esbuild in-memory bundle, 외부 RPC/DB/개인키/실서명 없음, 04의
`src/server/application/stamps/**`, `src/server/controllers/stamps/**`, `jobs/chain-indexer/**` provisional 코드를
in-memory repository/fake chain으로 그대로 로드). 원본 코드 무수정. 실행 로그: `docs/reports/qa/evidence/p3-probe.log`.

`node tests/e2e/p3-current.probe.mjs` → exit 1, 4건 위반 재현:

| id | 재현 | 실제 | 기대 |
|---|---|---|---|
|same-visit-new-key|같은 visit에 다른 idempotency key로 2회 발급 요청|서로 다른 nonce(1,2)로 각각 발급됨|같은 visit의 활성 승인을 재사용하거나 명시적으로 거절해야 함(같은 방문에 중복 서명 발급 가능성)|
|retry-with-valid-authorization-after-visit-ttl|승인 발급 직후 visit challenge TTL(300s) 경과 후 같은 idempotency key로 재시도|VISIT_EXPIRED 오류로 거절(claim deadline 1600은 아직 유효한데도)|이미 발급된 미만료 authorization은 방문 challenge 만료 여부와 무관하게 그대로 replay 되어야 함 - authorization TTL과 visit TTL 혼용|
|pending-hardcoded-false|controller가 statusService에 전달하는 `pending` 인자 관찰|항상 false로 고정 전달|영속화된 승인/tx 상태에서 유도해야 함|
|shorter-chain-stale-cursor|3블록 색인 후 블록1로 reorg(체인이 짧아짐)|`syncOnce()`가 `up_to_date` 반환, cursor는 여전히 2(실제 tip은 1)|고아 projection을 무효화하거나 명시적 unavailable 상태를 반환해야 함 - stale cursor를 up_to_date로 보고 금지|

P3-04/P3-06 handoff는 이미 provisional로 표시되어 있으나(in-memory test double 한계 명시), 이 4건은 in-memory
구현 자체의 로직 결함이라 실제 DB 연결 후에도 동일하게 재현될 것으로 예상한다. P3-04/P3-06 status를
"수정 필요"로 유지하며 04에게 수정 요구를 전달한다(dependency-requests.md P3-PROBE-04). 이 실행은 provisional
코드의 독립 재현이며 P3-07 실 통합 QA를 대체하지 않는다. 여전히 chain 배포/실서명/실DB/reorg 종단 검증은 미실행.
