# P3-06: 이벤트 동기화 인계

- agent_id: agent_04
- work_item_id: P3-06
- parent_tasks: [P3-06]
- run_id: agent04-20260924T113206+0900-4cb7ca8
- status: ready_for_review — **단, provisional로 표시**. `depends_on: [P3-02, P3-DB]` 중
  P3-DB가 아직 제출/수용되지 않아 in-memory repository test double로만 검증했다.
- base_commit: 4cb7ca83069f58103707426f215de5c96bddfe01
- result_commit_or_patch: shared checkout에 아래 신규 파일. commit 없음.
- changed_files: `jobs/chain-indexer/{ports,indexer,runLoop}.ts`,
  `jobs/chain-indexer/testing/{inMemoryIndexerRepository,fakeIndexerChain}.ts`,
  `jobs/chain-indexer/__tests__/indexer.test.ts`, `docs/reports/onchain/recovery.md`, 이 handoff.
  `src/server/providers/rpc/chainClient.ts`는 P3-04와 공유(중복 구현 없음, 변경 파일은
  P3-04 handoff에 기록).
- contract_versions: claim-v1.0.0-rc1(이벤트 스키마 기준)
- acceptance_results:
  - "재시작/중복 수집": 새 인덱서 인스턴스가 같은 repository를 이어받아도 중복 이벤트를
    만들지 않음을 실행 검증(아래).
  - "로컬 reorg 복구": 공통 조상 이후 이벤트/projection 취소, 이전 이벤트는 유지됨을 실행
    검증(아래). 개발 중 `applyCanonicalBatch`가 tip 블록 하나만 저장해 얕은 reorg도
    `DeepReorgError`로 오판하던 버그를 발견해 수정했다(`processedBlocks` 전체 구간 누적
    저장) — 상세는 `docs/reports/onchain/recovery.md` "개발 중 발견하고 고친 버그" 절.
  - "온체인 상태와 projection 비교": 구조상 가능(`getProjection` vs `hasStamp`)하나 별도
    대조 스크립트는 아직 작성하지 않음 — remaining_risks.
- verification_commands_and_results:
  - `npx vitest run jobs/chain-indexer` → **5 passing**(정상 수집/재시작 중복없음/
    confirmations 보류/RPC 장애/reorg 복구).
  - `npx tsc --noEmit` → exit 0.
  - **실제 로컬 Hardhat 노드 + 실제 배포 컨트랙트에 대한 종단 간 스모크**: 실제 `claim()`
    tx 제출 후 `runLoop.ts`를 실제로 기동해 `advanced fromBlock=1 toBlock=5 eventCount=1` →
    이후 `up_to_date` 안정화 → `SIGTERM`으로 진행 중 사이클을 마친 뒤 정상 종료함을 확인.
    로그 전문은 `docs/reports/onchain/recovery.md` 참조. **실 체인 reorg 재현(evm_revert)은
    시간 관계상 하지 못했다** — reorg 로직 자체는 위 fake-chain 단위 테스트로 검증.
- artifacts: `docs/reports/onchain/recovery.md`(알고리즘 설명, 버그 수정 내역, 실행 로그
  전문, acceptance 대조).
- dependency_requests:
  - `STAMPS-DB-04`(04→03, P3-04 handoff와 동일 요청 재사용): `jobs/chain-indexer/ports.ts`의
    `IndexerRepository`(event+projection+cursor 원자 commit, 공통 블록 rollback)를
    db/migrations(`chain_events`/`stamp_projections`/`indexer_cursors`)에 실제 구현.
  - `runLoop.ts`는 `INDEXER_REPOSITORY_MODULE` 환경변수로 03의 구현을 나중에 코드 변경
    없이 주입할 수 있게 설계했다 — 03 구현체가 준비되면 그 모듈 경로만 지정하면 된다.
- remaining_risks:
  - 03의 실제 DB 구현 전까지는 개발/시연 전용(재시작 시 커서/이벤트 전부 소실) — `runLoop.ts`가
    기동 시 매번 이 경고를 로그로 남긴다.
  - 실 체인 reorg 종단 간 재현 미실행.
  - `confirmations=0`(로컬)은 공개 배포 전 반드시 재검토 필요.
- next_actions: 03의 `IndexerRepository` 구현 완료 후 `INDEXER_REPOSITORY_MODULE`을 그
  구현으로 지정해 동일 테스트 스위트 + 실제 체인 스모크를 재실행하고 결과를 갱신 요청.
  01은 이번 provisional 코드/알고리즘 품질만 우선 검토.
