# Handoff: P1-05 (API 및 repository 인터페이스)

- agent_id: agent_03
- work_item_id: P1-05
- parent_tasks: [P1-05]
- run_id: a03-20260924-c0prep-01
- status: in_progress
- base_commit: 4cb7ca83069f58103707426f215de5c96bddfe01 (working tree상 전체 파일 diff는 줄바꿈 정규화뿐이며 내용 변경 없음 — `git diff --shortstat` = 동일 건수 insert/delete로 확인)
- result_commit_or_patch: 커밋하지 않음 (assigned_paths/checkout_path가 01로부터 공식 배정되지 않아 추가 전용(additive-only) 새 파일만 생성, 기존 파일 무수정). 아래 changed_files는 모두 신규 파일.
- changed_files:
  - src/shared/api/dto.ts (신규)
  - src/shared/api/index.ts (신규)
  - src/shared/api/check-no-server-import.mjs (신규, 정적 검사 스크립트)
  - src/server/repositories/planRepository.ts (신규, 인터페이스만)
  - src/server/repositories/inMemoryPlanRepository.ts (신규, mock 구현)
  - src/server/application/plans/validatePlannerInput.ts (신규, 형식 검증만)
  - src/server/__tests__/apiV1Contract.test.ts (신규)
  - docs/reports/backend/api-contract.md (신규, 조사+검토+dependency_request)
- contract_versions:
  - core-v1.0.0-rc1 (docs/contracts/core-v1.md, 상태 ready_for_review)
  - api-v1.0.0-rc1 (docs/contracts/api-v1.md, 상태 ready_for_review)
  - 위 두 계약 모두 agent_01의 accepted_handoffs에 아직 기록되지 않음 (`docs/reports/handoffs/agent_01/`가 비어 있고 `docs/reports/coordination/assignments.md`가 존재하지 않음을 확인)
- acceptance_results:
  - "잘못된 입력·오류 envelope·구 API 계약 테스트": **부분 충족**. mock repository + fixture 기반으로 8개 테스트 작성·통과(아래 검증 명령 참고). "구 API 계약 테스트"는 기존 `src/server/__tests__/planApi.test.ts`(5개, 기존 상태 그대로 통과)로 대체 확인했고, 신규 `/api/v1` 어댑터 자체는 아직 구현하지 않았다.
  - "src/server 구현을 브라우저에서 import하지 않음": **충족**. `node src/shared/api/check-no-server-import.mjs`로 정적 검증.
  - **미충족(다음 단계)**: 실제 `/api/v1` 라우트를 `src/server/app.ts`에 등록하는 것, PostgreSQL 연결(P1-06 영역), 레거시 어댑터 리팩터링. 이는 assigned_paths 공식 배정 및 C0 accepted_handoffs 이후로 미뤘다(§실행 근거 참고).
- verification_commands_and_results:
  - `node src/shared/api/check-no-server-import.mjs` → `OK: 2개 파일 중 src/server import 없음 (dto.ts, index.ts)` (exit 0)
  - `npx tsc --noEmit` (프로젝트 전체, strict) → exit 0, 오류 없음
  - `npx vitest run` (전체 스위트) → 3 files / 18 tests passed (기존 budgetCalculator.test.ts 5개, planApi.test.ts 5개 모두 그대로 통과 — 회귀 없음 확인. 신규 apiV1Contract.test.ts 8개 통과)
  - `npx vitest run src/server/__tests__/apiV1Contract.test.ts` → 8 tests passed (save 201/replay/conflict 409/missing 404/storage-down 503/RAG envelope 4종/invalid-input 2종)
  - 환경 메모: 이 checkout의 `node_modules`가 Windows 대상으로 설치되어 있어 로컬 Linux 실행 시 `@rollup/rollup-linux-x64-gnu`가 없어 vitest가 즉시 실패했다. `npm install @rollup/rollup-linux-x64-gnu --no-save`로 해당 세션에서만 임시 설치해 우회했다(`package.json`/`package-lock.json` diff 없음, `git diff --stat`로 확인 — 있는 diff는 전체 저장소에 공통인 줄바꿈 정규화뿐). 이 우회는 이 실행 환경에만 적용되며 저장소에는 반영하지 않았다. 02(패키지/락파일 소유자)에게 참고로 공유한다.
- artifacts:
  - docs/reports/backend/api-contract.md
  - src/shared/api/**, src/server/repositories/**, src/server/application/plans/validatePlannerInput.ts, src/server/__tests__/apiV1Contract.test.ts
- dependency_requests:
  -
    request_id: dep-a03-001
    from_agent: agent_03
    to_agent: agent_01
    consumer_work_item: P1-05
    requested_paths_or_interface: "docs/reports/coordination/assignments.md 작성 + C0(core-v1/api-v1) accepted_handoffs 기록"
    request_schema: "common_protocol.handoff 필드 형식과 동일"
    response_fixture: "해당 없음 (승인 기록 요청)"
    blocking_scope: "src/server/app.ts에 /api/v1 라우트를 실제 등록하는 다음 단계 전체"
    needed_before: "P1-05 status를 ready_for_review로 올리기 전"
    verification_expectation: "assignments.md에 agent_03의 base_commit/checkout_path/assigned_paths가 명시되고 agent_01의 accepted_handoffs 항목에 C0가 기록되어야 함"
  -
    request_id: dep-a03-002
    from_agent: agent_03
    to_agent: agent_01
    consumer_work_item: P1-05
    requested_paths_or_interface: "Idempotency-Key TTL 확정 (03 제안: 24시간, docs/reports/backend/api-contract.md §2-1 근거 참고)"
    request_schema: "docs/contracts/api-v1.md 개정 또는 별도 확정 메모"
    response_fixture: "fixtures/api-v1.json에 만료 후 재사용 케이스 추가 요청"
    blocking_scope: "PlanRepository.saveIdempotent의 만료 정책 구현"
    needed_before: "P1-06 착수 전"
    verification_expectation: "01이 TTL 값을 승인/수정하고 fixture에 반영"
- remaining_risks:
  - mock repository는 동시성(같은 키 동시 요청)을 테스트하지 않았다 — 실제 DB transaction/UNIQUE 제약이 있어야 검증 가능(P1-06).
  - `validatePlannerInput`의 범위(1..1,000,000 / 1..8)는 core-v1.md가 "정책 미확정"이라 명시한 draft 값이다. 정책 확정 시 이 파일만 고치면 되도록 상수로 분리했으나, 값 자체가 바뀔 수 있다.
  - 레거시 `/api/plans` 어댑터 통합 방향(§api-contract.md §2-2)은 제안 단계이며 01/02 검토 전이다.
- next_actions:
  - agent_01: assignments.md 작성 + C0 accepted_handoffs 기록, TTL 제안 승인/수정.
  - agent_03(수용 후): `/api/v1` 라우트를 app.ts에 등록, 레거시 어댑터 구현, PlanRepository 실제 사용 지점 연결(P1-02 엔진 계약 인계 후).
