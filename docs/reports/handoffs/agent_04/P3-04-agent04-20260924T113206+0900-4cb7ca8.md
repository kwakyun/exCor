# P3-04: 지갑 인증·방문 승인·claim API 인계

- agent_id: agent_04
- work_item_id: P3-04
- parent_tasks: [P3-04]
- run_id: agent04-20260924T113206+0900-4cb7ca8
- status: ready_for_review — **단, provisional로 표시**. `depends_on: [P3-01, P3-DB]` 중
  P3-DB(03의 실제 repository)가 아직 제출/수용되지 않았다. 04-onchain.yaml
  `readiness.provisional_work`에 따라 in-memory repository test double로만 검증했으며
  실제 연동 완료가 아니다.
- base_commit: 4cb7ca83069f58103707426f215de5c96bddfe01
- result_commit_or_patch: shared checkout에 아래 신규 파일. commit 없음.
- changed_files: `src/server/application/stamps/{ports,errors,walletAuthService,visitService,
  authorizationService,statusService,systemClock}.ts`,
  `src/server/application/stamps/testing/{inMemoryStampsRepository,fakeChainReader}.ts`,
  `src/server/application/stamps/__tests__/{walletAuthService,visitService,
  authorizationService,statusService}.test.ts`,
  `src/server/controllers/stamps/{httpEnvelope,stampsController}.ts`,
  `src/server/providers/signing/{claimTypedData,issuerSigner}.ts`,
  `src/server/providers/signing/__tests__/{claimTypedData,issuerSigner}.test.ts`,
  `src/server/providers/rpc/chainClient.ts`, `docs/reports/onchain/server-api.md`, 이 handoff.
  **`src/server/app.ts`/DB migration/루트 lockfile은 수정하지 않았다** — route 등록은 03의 몫.
- contract_versions: api-v1.0.0-rc3(envelope/에러코드), claim-v1.0.0-rc1(typed data)
- acceptance_results:
  - "미승인/다른 지갑 요청 차단": 세션 없음/만료→`UNAUTHENTICATED`, 서명 불일치→`FORBIDDEN`,
    타 지갑 소유 방문→`FORBIDDEN`, 미승인/만료 방문→`VISIT_NOT_APPROVED`/`VISIT_EXPIRED`.
    단위 테스트로 검증(아래 verification).
  - "멱등 재시도 및 동시 요청 검증": 동일 키+동일 본문→replayed(재서명 안 함), 동일 키+
    다른 본문→409, repository conflict→409로 안전 처리. **실제 DB 동시성(트랜잭션 격리)은
    미검증** — in-memory 더블의 한계로 remaining_risks에 명시.
  - "서명 발급과 tx 확정을 구분": `statusService`는 서명 발급 여부가 아니라 온체인
    `hasStamp`로 confirmed를 판정. `authorizationService`는 서명까지만 책임.
  - 상세 근거는 `docs/reports/onchain/server-api.md`의 "acceptance 대조" 절.
- verification_commands_and_results:
  - `npx vitest run src/server/application/stamps src/server/providers/signing` →
    `walletAuthService.test.ts`(5) + `visitService.test.ts`(6) + `authorizationService.test.ts`(7)
    + `statusService.test.ts`(4) + `claimTypedData.test.ts`(2) + `issuerSigner.test.ts`(2) =
    **26 passing**.
  - `npx tsc --noEmit` → exit 0.
  - 이 26개 테스트는 in-memory repository/fake RPC/테스트 서명자로만 검증했다 — 실제
    PostgreSQL/실제 RPC 연동은 하지 않았다(제목의 provisional 표시와 동일한 의미).
- artifacts: `docs/reports/onchain/server-api.md`(route 표, acceptance 대조,
  `StampsRepository`/`Clock`/`NonceGenerator` 인터페이스 요약).
- dependency_requests:
  - `STAMPS-DB-04`(04→03): `src/server/application/stamps/ports.ts`의 `StampsRepository`를
    db/migrations + `src/server/repositories/**`에 실제 구현. 상세 스키마/blocking_scope/
    verification_expectation은 `docs/reports/onchain/server-api.md` "03에게 보내는
    dependency_request" 절 참조. needed_before: P3-04를 provisional에서 실제 연동으로
    전환하기 전.
  - 03에게: `createStampsController` factory(`src/server/controllers/stamps/stampsController.ts`)와
    `stampsRouteTable`을 `app.ts`에 등록해달라(04는 app.ts를 수정하지 않음).
- remaining_risks: 실제 DB 동시성 미검증; TTL 상수(5/15/5/10분)는 로컬 초안이며 공개 환경
  적용 전 사람 승인 필요; `MockVisitVerifierAuthorizer`는 데모 전용이며 실 verifier 인증
  정책 미결정; `STAMP_ISSUER_PRIVATE_KEY` 환경변수 부재 시 `ConfigMissingError`로 즉시
  실패하도록 했으나 실제 프로세스 비밀 관리(secret manager) 연동은 범위 밖.
- next_actions: 03이 `StampsRepository` 구현과 app.ts route 등록을 완료하면, 04가 실제 DB에
  대해 재검증(in-memory→실제 repository로 교체 후 동일 테스트 스위트 재실행)한 뒤 이
  work item의 status를 실제 연동 근거와 함께 갱신 요청. 01은 이번 provisional 코드 품질만
  우선 검토.
