# Handoff: P1-05 (배정 수신 확인 + rc3 동기화) — BACKEND-HANDOFF-03 1차 응답

- agent_id: agent_03
- work_item_id: P1-05
- parent_tasks: [P1-05]
- run_id: agent03-20260924T105400+0900-4cb7ca8
- status: in_progress
- base_commit: 4cb7ca83069f58103707426f215de5c96bddfe01 (assignments.md와 동일 base_commit 확인)
- result_commit_or_patch: 커밋하지 않음(공유 checkout, 01 지시대로 전체 stage/commit 금지). 아래 changed_files는 모두 additive 신규/수정.
- changed_files:
  - src/shared/api/dto.ts (수정: RecommendationRequestDTO.constraints를 `Partial<PlannerInputDTO>`로 변경, rc3 동기화)
  - src/server/application/plans/validatePlannerInput.ts (수정: `checkRecommendationConstraints()` 추가)
  - src/server/application/plans/normalizeRequestHash.ts (신규: 정규화 요청 해시)
  - src/server/repositories/planRepository.ts (수정: `IDEMPOTENCY_KEY_TTL_HOURS` 상수 + 경계 규칙 문서화 + `SavedPlanRecord.expiresAt` 필드 추가)
  - src/server/repositories/inMemoryPlanRepository.ts (수정: 시계 주입 + TTL 만료 분기)
  - src/server/__tests__/apiV1Contract.test.ts (수정: rc3 requestCases 4종 + TTL 경계 2종 + hash 정규화 1종 테스트 추가, 총 16개)
  - docs/reports/backend/api-contract.md (수정: §7 rc2/rc3 동기화, §8 관찰사항 추가)
  - docs/reports/backend/db-namespace-plan.md (신규)
- contract_versions:
  - core-v1.0.0-rc2 (반영 완료 — 내가 쓰는 부분은 budgetKRW/partySize 범위·카탈로그 구조로 변경 없음, swapCases 메타데이터 변경은 내 코드에 영향 없음 확인)
  - api-v1.0.0-rc3 (반영 완료 — 아래 acceptance_results 참고)
  - 여전히 accepted_handoffs 없음(ready_for_review) — 실제 route/DB 연결은 계속 보류.
- acceptance_results:
  - rc3 requestCases 4종: missing-budget-party(clarify)/invalid-provided-party(400)/invalid-party-type(400) **충족**. conflicting-budget(clarify)은 **의도적으로 미충족** — NL 파싱이 필요해 이 계층(DTO/형식검증) 밖이며 P2-04(extractConstraints) 몫임을 테스트 코드와 문서에 명시했다. 꾸며서 통과시키지 않았다.
  - Idempotency TTL 24h: 경계값(23:59:59.999 / 24:00:00.000) 두 지점 모두 fixture로 재현·통과. 여전히 **로컬 mock 제안**이며 01의 최종 승인 대기.
  - normalizeRequestHash: 키 순서 무관/값 변화 민감 두 속성 모두 테스트로 확인.
- verification_commands_and_results:
  - `npx tsc --noEmit` → exit 0
  - `npx vitest run src/server/__tests__/apiV1Contract.test.ts src/server/__tests__/planApi.test.ts src/services/__tests__/budgetCalculator.test.ts` → 3 files / 26 tests passed
  - (참고, 내 소유 아님) `npx vitest run`(전체, 인자 없음) → chain/node_modules.broken.1790214643/zod/** 테스트 3개 스위트 fail, 내 파일과 무관. 02/01에게 §관찰 사항으로 별도 보고(docs/reports/backend/api-contract.md §8). 04가 최근(01:49~01:50) chain/ 작업 중인 것으로 보여 내가 그 디렉터리를 건드리지 않았다.
- artifacts:
  - docs/reports/backend/api-contract.md, docs/reports/backend/db-namespace-plan.md
  - src/shared/api/**, src/server/repositories/**, src/server/application/plans/**, src/server/__tests__/apiV1Contract.test.ts
- dependency_requests:
  -
    request_id: dep-a03-005
    from_agent: agent_03
    to_agent: agent_01
    consumer_work_item: P1-05
    requested_paths_or_interface: "IDEMPOTENCY_KEY_TTL_HOURS=24 및 경계 규칙(§api-contract.md §7-3) 승인 또는 수정 지시"
    request_schema: "docs/contracts/api-v1.md 개정(rc4) 또는 별도 승인 기록"
    response_fixture: "fixtures/api-v1.json에 만료 후 재사용 case 추가 여부는 01 판단에 맡김"
    blocking_scope: "P1-06 실제 DB 스키마의 expires_at 컬럼/인덱스 설계"
    needed_before: "P1-06 착수 전"
    verification_expectation: "01의 명시적 승인/수정 코멘트가 handoff 또는 assignments.md에 기록"
  -
    request_id: dep-a03-006
    from_agent: agent_03
    to_agent: agent_02
    consumer_work_item: P1-05
    requested_paths_or_interface: "루트 vitest 설정(vite.config.ts 또는 vitest.config)에 chain/** 명시적 제외 추가 검토"
    request_schema: "해당 없음, 참고 보고"
    response_fixture: "해당 없음"
    blocking_scope: "npm test(전체) 실행 시 chain/node_modules.broken.*/zod 테스트가 fail로 잡혀 CI 신호를 오염시킬 수 있음"
    needed_before: "CI 워크플로 확정 전"
    verification_expectation: "02가 확인 후 필요 시 test.exclude 추가"
- remaining_risks:
  - conflicting-budget 케이스는 여전히 미해결 — P2-04 인계 전까지 이 계층에서 clarify로 자동 승격되지 않는다(의도적, 문서화됨).
  - TTL/해시 설계는 공유 checkout에서 다른 담당자가 같은 파일을 아직 건드리지 않았음을 git status로만 확인했다(commit 이력으로 확정된 것은 아님) — 01의 통합 창 조정을 따른다.
  - db-namespace-plan.md의 migration 번호 대역은 제안일 뿐이며 04의 실제 P3-01 스키마 요청 전까지 0030 대역 내용은 비어 있다.
- next_actions:
  - agent_01: TTL 제안 승인/수정, 이번 배정 수신을 원장에 반영.
  - agent_02: chain/ vitest 누수 확인(참고), P1-02 인계 시 도메인 타입 export 방식 논의.
  - agent_04: P3-01 repository 요청을 구체 스키마로 보내면 03이 db-namespace-plan.md의 0030 대역에서 실제 migration을 설계한다.
  - agent_03: C0 accepted_handoffs 및 P1-02/ENV-01 인계를 기다리며 다음 ready 작업(P2-01 후속: 외부 출처 승인 대기, 또는 P3-DB 스키마 설계 착수 가능 여부)을 확인한다.
